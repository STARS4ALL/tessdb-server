Todas y cada una de estas cosas en su rama git
=============================================
*) Actualizar doc de oepracion
*) sunrise / sunset especifico por instrumento
-comprobar la vercadidad de los calculos
- que pasa con los nulos de longitude, latitude elevation
*) Bug de MQTT. Se muere la factoría del protocolo.


*) Control dethreads con suggestThreadPool
*) Control del crecimeinto de la cola. 
  si average > 0 + 3*stddev => log warn.
    from __future__ import division
	def average(L):
		return sum(L) / len(L)

	def variance(L, average):
		return  sum((average - value) ** 2 for value in L) / len(L)-1
*) Logrotate en la Pi al mediodia
Con muy baja prioridad si es que es interesante:
*) Etiquetas UNKNOWN, CURRENT, EXPIRED configurables por idoioma en una sección [locale] del fichero de configuracion
*) Embeber servidor web de twisted.
